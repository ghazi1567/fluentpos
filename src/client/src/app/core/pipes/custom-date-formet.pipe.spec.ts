import { CustomDateFormetPipe } from './custom-date-formet.pipe';

describe('CustomDateFormetPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateFormetPipe();
    expect(pipe).toBeTruthy();
  });
});
